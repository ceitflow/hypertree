export class Router {

  init() {
    // 1. pack all nodes in a quadtree (immediate children only)
    // 2. build graph:
    //   - vertices are touching points between circles
    //   - links are visibility graph along curves
    // 3. sum thickness for each segments
  }
}
