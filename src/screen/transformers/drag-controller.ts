// todo drag transformer? zoom out if too close, translate screen with drag when near the edge

// else
//  dragHandler.move() // one or many elements
//  todo calculate myself constraint force? mousePos vs actualElementPos
//  elementPos.inverse to compare with clientX Y

/*
// todo squish animation (apple like)
// animation transformer can be self correcting (if user translates it compensates with some delay)
// output
// - viewport
// - visible cells (quadtree)
*/
